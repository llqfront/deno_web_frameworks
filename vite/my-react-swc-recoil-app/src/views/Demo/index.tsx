import React, { memo } from 'react';
interface Props {
}
const View: React.FC = (props: Props) => {

    return (
      <React.Fragment>
        demo
      </React.Fragment>
    )
}
export default memo(View);
